import { permissions } from "../helpers/constants.js"

export const settings = {
    name: "nba",
    description: "WIP",
    enabled: true,
    permission: permissions.VIEWER,
    globalDelay: 0,
    userDelay: 0,
    dailyLimit: 70,
}
